# Clawdbot 启动脚本修复说明

## 问题原因
所有启动脚本都使用了错误的路径，指向 `D:\Git_GitHub\clawdbot`，但实际项目在 `E:\myclawdbot`。

## 修复内容

### 1. `.A_Build-All.cmd` ✅ 已修复
- 修复路径：`D:\Git_GitHub\clawdbot` → `E:\myclawdbot`
- 功能：完整构建（TypeScript + UI）
- 测试结果：✅ 正常工作

### 2. `.A_Start-Clawdbot-Fast-Fixed.ps1` ✅ 新建并增强
- 修复路径：`D:\Git_GitHub\clawdbot` → `E:\myclawdbot`
- 修复PID变量冲突
- **新增智能构建检测**：
  - TypeScript构建检测（源文件变更检查）
  - UI构建检测（UI源文件变更检查）
  - 仅在需要时构建，节省时间
- **新增自动功能**：
  - 自动清理端口18789
  - 启动后自动打开带token的Control UI
  - 在新窗口启动Gateway日志

### 3. `.A_Start-Clawdbot.cmd` ✅ 已修复
- 更新为调用修复后的PowerShell脚本
- 测试结果：✅ 正常工作

## 使用方法

### 方法1：一键启动（推荐）
```cmd
.A_Start-Clawdbot.cmd
```
**功能**：
- ✅ 智能检测并构建TypeScript（如需要）
- ✅ 智能检测并构建UI（如需要）
- ✅ 自动清理端口18789
- ✅ 在新窗口启动Gateway
- ✅ 自动打开带token的Control UI

### 方法2：完整构建后启动
```cmd
.A_Build-All.cmd    # 强制完整构建
.A_Start-Clawdbot.cmd  # 启动（会跳过已构建的内容）
```

### 方法3：直接使用PowerShell脚本
```powershell
powershell -ExecutionPolicy Bypass -File ".A_Start-Clawdbot-Fast-Fixed.ps1"
```

## 新增功能说明

### 智能构建检测
- **TypeScript检测**：检查src文件、tsconfig.json、package.json的时间戳
- **UI检测**：检查ui源文件与构建产物的时间戳
- **构建优化**：仅在需要时构建，避免重复构建

### 自动打开Dashboard
- 启动Gateway后等待2秒
- 自动打开带认证token的Control UI
- URL: `http://127.0.0.1:18789/?token=07f14e7c946cd9b4cd521eca7dc602e8560dcfbeb92c0013`

## 访问地址
- **主界面**: http://127.0.0.1:18789/?token=07f14e7c946cd9b4cd521eca7dc602e8560dcfbeb92c0013
- **Canvas画板**: http://127.0.0.1:18789/__clawdbot__/canvas/
- **浏览器控制**: http://127.0.0.1:18791/

## 注意事项
- 确保已安装pnpm和Node.js
- 确保项目依赖已安装（`pnpm install`）
- 脚本会自动处理构建，无需手动构建
- Gateway会在新窗口运行，便于查看日志
