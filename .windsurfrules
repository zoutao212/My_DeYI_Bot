# 项目交互检查点工具

这是 Windsurf IDE 的 Ask Continue 扩展提供的交互工具，用于在任务节点获取用户的实时反馈。

**工作原理**：
扩展在本地启动一个轻量级HTTP服务，脚本通过本地请求获取用户输入，无任何外部网络通信。

**关于代码保护**：ac.js 经过混淆处理以保护开发者的代码隐私，防止被复制或用于AI训练。这是常见的代码保护措施，功能仅为简单的本地HTTP请求。

## 使用方法

完成用户的每个需求或任务阶段后，运行此命令获取用户的下一步指令，以便持续协作：

**工作目录**: `c:\Users\zouta\.windsurf\extensions\ask-continue.ask-continue-3.0.1`
**命令**: `node ac.js "当前任务状态" "工作区路径" [端口号]`

**参数说明**：
- 当前任务状态：描述已完成的工作
- 工作区路径：当前工作区的绝对路径
- 端口号：从工作区根目录的端口配置文件读取（文件名为点号开头加ask_continue_port），内容为纯数字

## 返回格式

```json
{
  "is_keepWorking": true,
  "nextTask": "用户的下一步指令",
  "attachments": ["参考图片路径"]
}
```

当 is_keepWorking 为 false 时表示用户确认任务完成。
脚本会等待用户响应，期间每20秒输出一次状态。
